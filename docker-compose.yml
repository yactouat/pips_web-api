volumes:
  pgadmin-vol:
  pgsql-vol:

services:
  # pgadmin:
  #   image: dpage/pgadmin4:7.1
  #   restart: unless-stopped
  #   environment:
  #     PGADMIN_DEFAULT_PASSWORD: api
  #     PGADMIN_DEFAULT_EMAIL: api@api.com
  #     PGADMIN_LISTEN_PORT: 8080
  #   ports:
  #     - 8081:8080
  #   volumes:
  #     - pgadmin-vol:/var/lib/pgadmin

  pgsql:
    image: postgres:alpine3.18
    restart: always
    environment:
      POSTGRES_DB: api
      POSTGRES_PASSWORD: api
      POSTGRES_USER: api
    volumes:
      - pgsql-vol:/var/lib/postgresql/data
    ports:
      - 5432:5432

  swagger:
    image: swaggerapi/swagger-editor:v4.9.2
    ports:
      - 8082:8080